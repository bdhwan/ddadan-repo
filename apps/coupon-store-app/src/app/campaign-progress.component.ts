import{HttpClient,HttpParams}from'@angular/common/http';import{ChangeDetectionStrategy,Component,DestroyRef,OnInit,inject,signal}from'@angular/core';import{takeUntilDestroyed}from'@angular/core/rxjs-interop';import type{OwnerCampaignProgressDto,OwnerCampaignProgressListResponseDto}from'@coupon/contracts';import{visibilityAwarePoll}from'@coupon/client-core';import{formatKoreaDateTime}from'@coupon/domain';import{CouponBadgeComponent,CouponCardComponent,CouponEmptyStateComponent,CouponErrorStateComponent,CouponPageHeaderComponent,CouponSkeletonComponent}from'@coupon/ui';
@Component({selector:'coupon-store-campaign-progress',imports:[CouponBadgeComponent,CouponCardComponent,CouponEmptyStateComponent,CouponErrorStateComponent,CouponPageHeaderComponent,CouponSkeletonComponent],changeDetection:ChangeDetectionStrategy.OnPush,template:`<coupon-page-header title="할인 캠페인" description="발급 대상 스냅샷과 실제 처리 수를 구분해 5초마다 갱신합니다." eyebrow="발급 진행"/>@if(loading()&&!version()){<coupon-card><coupon-skeleton [lines]="7" label="발급 진행 상황을 불러오는 중입니다."/></coupon-card>}@else if(error()&&!version()){<coupon-error-state title="캠페인을 불러오지 못했어요" [description]="error()!" [retryable]="true" (retry)="load()"/>}@else if(items().length===0){<coupon-empty-state title="진행 중인 캠페인이 없어요" description="새 캠페인을 게시하면 대상 확정과 처리 진행률이 이곳에 표시됩니다."/>}@else{<div class="grid">@for(c of items();track c.id){<article><div><coupon-badge [status]="badge(c.status)" [label]="label(c.status)">{{label(c.status)}}</coupon-badge><time>{{date(c.updated_at)}}</time></div><h2>{{c.name}}</h2><dl><div><dt>대상 스냅샷 확정</dt><dd>{{c.snapshot_target_count===null?'확정 중':c.snapshot_target_count+'명'}}</dd></div><div><dt>처리 완료</dt><dd>{{c.processed_count}}명</dd></div><div><dt>발급 성공</dt><dd>{{c.issued_count}}건</dd></div><div><dt>실패</dt><dd>{{c.failed_count}}건</dd></div></dl><div class="progress" role="progressbar" [attr.aria-label]="c.name+' 발급 처리율'" [attr.aria-valuenow]="percent(c)" aria-valuemin="0" aria-valuemax="100"><span [style.width.%]="percent(c)"></span></div><p>{{percent(c)}}% 처리 · 브라우저가 숨겨지면 폴링을 중단합니다.</p></article>}</div>}`,styles:`:host{display:block}.grid{display:grid;gap:1rem}.grid article{padding:1rem;border:1px solid var(--coupon-color-border);border-radius:var(--coupon-radius-md);background:var(--coupon-color-surface)}article>div:first-child{display:flex;align-items:center;justify-content:space-between}time,article>p{color:var(--coupon-color-text-muted);font-size:.875rem}h2{margin:.8rem 0}dl{display:grid;grid-template-columns:1fr 1fr;margin:0}dl div{padding:.55rem;border-bottom:1px solid var(--coupon-color-border)}dt{color:var(--coupon-color-text-muted)}dd{margin:0;font-weight:800}.progress{height:.75rem;margin-top:1rem;overflow:hidden;border-radius:1rem;background:var(--coupon-color-surface-muted)}.progress span{display:block;height:100%;background:var(--coupon-color-primary)}@media(min-width:768px){.grid{grid-template-columns:repeat(2,1fr)}}`})
export class CampaignProgressComponent implements OnInit{private readonly http=inject(HttpClient);private readonly destroyRef=inject(DestroyRef);private inFlight=false;readonly items=signal<OwnerCampaignProgressDto[]>([]);readonly loading=signal(true);readonly error=signal<string|null>(null);readonly version=signal<number|null>(null);readonly updatedAt=signal<string|null>(null);ngOnInit():void{visibilityAwarePoll(5_000).pipe(takeUntilDestroyed(this.destroyRef)).subscribe(()=>this.load());}load():void{if(this.inFlight)return;this.inFlight=true;let params=new HttpParams();if(this.version()!==null)params=params.set('version',this.version()!);if(this.updatedAt())params=params.set('updated_at',this.updatedAt()!);this.http.get<OwnerCampaignProgressListResponseDto>('/api/coupon/v1/owner/campaigns',{params}).pipe(takeUntilDestroyed(this.destroyRef)).subscribe({next:r=>{this.items.set(r.items);this.version.set(r.version);this.updatedAt.set(r.updated_at);this.loading.set(false);this.error.set(null);this.inFlight=false;},error:()=>{this.error.set('서버 연결을 확인해 주세요.');this.loading.set(false);this.inFlight=false;}});}percent(c:OwnerCampaignProgressDto):number{return c.snapshot_target_count?Math.min(100,Math.round(c.processed_count/c.snapshot_target_count*100)):0;}date(v:string):string{return formatKoreaDateTime(v);}label(v:OwnerCampaignProgressDto['status']):string{return({DRAFT:'초안',SCHEDULED:'예약',ISSUING:'발급 중',PAUSED:'일시 중지',ENDED:'종료',CANCELLED:'취소'})[v];}badge(v:OwnerCampaignProgressDto['status']):'success'|'warning'|'danger'|'neutral'{return v==='ISSUING'?'success':v==='SCHEDULED'||v==='PAUSED'?'warning':v==='CANCELLED'?'danger':'neutral';}}
