export const environment = {
  production: true,
  firebase: {
    apiKey: 'replace-at-deploy',
    authDomain: 'replace-at-deploy',
    projectId: 'replace-at-deploy',
    appId: 'replace-at-deploy',
  },
} as const;
